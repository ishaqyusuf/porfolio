# Coding Standards

## Purpose
Tracks implementation standards.

## Standards
- Use TypeScript and strict typing.
- Keep content separate from presentation.
- Use shadcn-compatible component conventions.
- Keep styling tokenized in `app/globals.css`.
- Prefer semantic HTML and accessible links.
- Keep components small and portfolio-specific unless they become reusable primitives.
